diff --git a/node_modules/react-native-drawer-layout/lib/module/views/Drawer.js b/node_modules/react-native-drawer-layout/lib/module/views/Drawer.js
index af7d7a9..fa73d0a 100644
--- a/node_modules/react-native-drawer-layout/lib/module/views/Drawer.js
+++ b/node_modules/react-native-drawer-layout/lib/module/views/Drawer.js
@@ -46,8 +46,9 @@ export function Drawer({
       element?.removeEventListener('transitionend', onTransitionEndLatest);
     };
   }, [onTransitionEndLatest, onTransitionStartLatest]);
-  const isOpen = drawerType === 'permanent' ? true : open;
   const isRight = drawerPosition === 'right';
+  /** Collapsed rail for permanent drawer when navigation marks it closed */
+  const permCollapsed = drawerType === 'permanent' && !open;
   const drawerTranslateX =
   // The drawer stays in place at open position when `drawerType` is `back`
   open || drawerType === 'back' ? drawerPosition === 'left' ? '100%' : '-100%' : 0;
@@ -55,6 +56,22 @@ export function Drawer({
     transition: 'transform 0.3s',
     transform: `translateX(${drawerTranslateX})`
   } : null;
+  /** Permanent: clip outer width while inner panel slides left (no content squish). */
+  const permOuterSlide = drawerType === 'permanent' ? {
+    width: permCollapsed ? 0 : drawerWidth,
+    overflow: 'hidden',
+    transition: 'width 0.36s cubic-bezier(0.4, 0, 0.2, 1)',
+    minWidth: 0,
+    pointerEvents: permCollapsed ? 'none' : 'auto'
+  } : null;
+  const permInnerSlide = drawerType === 'permanent' ? {
+    width: drawerWidth,
+    maxWidth: drawerWidth,
+    flexShrink: 0,
+    alignSelf: 'flex-start',
+    transform: permCollapsed ? 'translateX(-100%)' : 'translateX(0)',
+    transition: 'transform 0.36s cubic-bezier(0.4, 0, 0.2, 1)'
+  } : null;
   const contentTranslateX = open ?
   // The screen content stays in place when `drawerType` is `front`
   drawerType === 'front' ? 0 : `calc(${drawerWidth} * ${drawerPosition === 'left' ? 1 : -1})` : 0;
@@ -68,10 +85,13 @@ export function Drawer({
       position: drawerType === 'permanent' ? 'relative' : 'absolute',
       zIndex: drawerType === 'back' ? -1 : 1
     },
+    drawerType !== 'permanent' ?
     // FIXME: width contains `px` on web
     {
-      width: drawerWidth
-    },
+      width: permCollapsed ? 0 : drawerWidth,
+      overflow: permCollapsed ? 'hidden' : 'visible',
+      pointerEvents: permCollapsed ? 'none' : 'auto'
+    } : null,
     // @ts-expect-error offset contains `calc` for web
     drawerType !== 'permanent' ?
     // Position drawer off-screen by default in closed state
@@ -81,13 +101,29 @@ export function Drawer({
       right: `calc(${drawerWidth} * -1)`
     } : {
       left: `calc(${drawerWidth} * -1)`
-    } : null, drawerAnimatedStyle, drawerStyle],
-    children: renderDrawerContent()
+    } : null,
+    // drawerStyle includes width: N — merge BEFORE permOuterSlide so clip width wins when collapsed
+    drawerStyle,
+    drawerType === 'permanent' ? permOuterSlide : null,
+    drawerType === 'permanent' ? null : drawerAnimatedStyle,
+    drawerType === 'permanent' && permCollapsed ? {
+      padding: 0,
+      margin: 0,
+      borderWidth: 0,
+      borderRightWidth: 0,
+      borderLeftWidth: 0
+    } : null],
+    children: drawerType === 'permanent' ? /*#__PURE__*/_jsx(View, {
+      style: permInnerSlide,
+      children: renderDrawerContent()
+    }) : renderDrawerContent()
   }, "drawer");
   const mainContent = /*#__PURE__*/_jsxs(View, {
     style: [styles.content, contentAnimatedStyle],
     children: [/*#__PURE__*/_jsx(View, {
-      "aria-hidden": isOpen && drawerType !== 'permanent',
+      // Never mark the workspace as aria-hidden: side drawers are non-modal and
+      // should remain fully interactive (slide/front previously set true while open).
+      "aria-hidden": false,
       style: styles.content,
       children: children
     }), drawerType !== 'permanent' ? /*#__PURE__*/_jsx(Overlay, {
@@ -115,10 +151,11 @@ const styles = StyleSheet.create({
     top: 0,
     bottom: 0,
     maxWidth: '100%',
-    backgroundColor: 'white'
+    backgroundColor: 'transparent'
   },
   content: {
-    flex: 1
+    flex: 1,
+    minWidth: 0
   }
 });
 //# sourceMappingURL=Drawer.js.map
diff --git a/node_modules/react-native-drawer-layout/lib/module/views/Drawer.native.js b/node_modules/react-native-drawer-layout/lib/module/views/Drawer.native.js
index c5882f9..43022d1 100644
--- a/node_modules/react-native-drawer-layout/lib/module/views/Drawer.native.js
+++ b/node_modules/react-native-drawer-layout/lib/module/views/Drawer.native.js
@@ -2,7 +2,7 @@
 
 import * as React from 'react';
 import { I18nManager, InteractionManager, Keyboard, Platform, StatusBar, StyleSheet, useWindowDimensions, View } from 'react-native';
-import Animated, { interpolate, ReduceMotion, runOnJS, useAnimatedStyle, useDerivedValue, useSharedValue, withSpring } from 'react-native-reanimated';
+import Animated, { Easing, interpolate, ReduceMotion, runOnJS, useAnimatedStyle, useDerivedValue, useSharedValue, withSpring, withTiming } from 'react-native-reanimated';
 import useLatestCallback from 'use-latest-callback';
 import { DrawerGestureContext } from "../utils/DrawerGestureContext.js";
 import { DrawerProgressContext } from "../utils/DrawerProgressContext.js";
@@ -53,6 +53,23 @@ export function Drawer({
     layout,
     drawerStyle
   });
+  const permDrawerWidth = useSharedValue(
+    drawerType === 'permanent' ? (open ? drawerWidth : 0) : drawerWidth
+  );
+  const permDrawerInnerX = useSharedValue(
+    drawerType === 'permanent' ? (open ? 0 : -drawerWidth) : 0
+  );
+  React.useEffect(() => {
+    if (drawerType !== 'permanent') {
+      return;
+    }
+    const timing = {
+      duration: 360,
+      easing: Easing.bezier(0.4, 0, 0.2, 1)
+    };
+    permDrawerWidth.value = withTiming(open ? drawerWidth : 0, timing);
+    permDrawerInnerX.value = withTiming(open ? 0 : -drawerWidth, timing);
+  }, [open, drawerWidth, drawerType]);
   const isOpen = drawerType === 'permanent' ? true : open;
   const isRight = drawerPosition === 'right';
   const getDrawerTranslationX = React.useCallback(open => {
@@ -113,7 +130,9 @@ export function Drawer({
   }, [isRight, isOpen, swipeEdgeWidth]);
   const touchStartX = useSharedValue(0);
   const touchX = useSharedValue(0);
-  const translationX = useSharedValue(getDrawerTranslationX(open));
+  const translationX = useSharedValue(
+    getDrawerTranslationX(drawerType === 'permanent' ? true : open)
+  );
   const gestureState = useSharedValue(GestureState.UNDETERMINED);
   const onAnimationStart = useLatestCallback(open => {
     onTransitionStart?.(!open);
@@ -149,7 +168,12 @@ export function Drawer({
       runOnJS(onClose)();
     }
   }, [getDrawerTranslationX, onAnimationEnd, onAnimationStart, onClose, onOpen, touchStartX, touchX, translationX]);
-  React.useEffect(() => toggleDrawer(open), [open, toggleDrawer]);
+  React.useEffect(() => {
+    if (drawerType === 'permanent') {
+      return;
+    }
+    toggleDrawer(open);
+  }, [open, toggleDrawer, drawerType]);
   const startX = useSharedValue(0);
   const pan = React.useMemo(() => {
     let panGesture = Gesture?.Pan().onBegin(event => {
@@ -220,6 +244,12 @@ export function Drawer({
   });
   const drawerAnimatedStyle = useAnimatedStyle(() => {
     const distanceFromEdge = layout.width - drawerWidth;
+    if (drawerType === 'permanent') {
+      return {
+        zIndex: translateX.get() === 0 ? 0 : 1,
+        transform: []
+      };
+    }
     return {
       // FIXME: Reanimated skips committing to the shadow tree if no layout props are animated
       // This results in pressables not getting their correct position and can't be pressed
@@ -227,16 +257,33 @@ export function Drawer({
       // For 'back' type: always keep drawer behind content (-1 when open, 0 when closed/animating)
       // For other types: 0 when open, 1 when closed/animating
       zIndex: drawerType === 'back' ? translateX.get() === 0 ? -1 : 0 : translateX.get() === 0 ? 0 : 1,
-      transform: drawerType === 'permanent' ?
-      // Reanimated needs the property to be present, but it results in Browser bug
-      // https://bugs.chromium.org/p/chromium/issues/detail?id=20574
-      [] : [{
+      transform: [{
         translateX:
         // The drawer stays in place when `drawerType` is `back`
         (drawerType === 'back' ? 0 : translateX.value) + (direction === 'rtl' ? drawerPosition === 'left' ? -distanceFromEdge : 0 : drawerPosition === 'left' ? 0 : distanceFromEdge)
       }]
     };
   }, [direction, drawerPosition, drawerType, drawerWidth, layout.width, translateX]);
+  const permClipStyle = useAnimatedStyle(() => {
+    if (drawerType !== 'permanent') {
+      return {};
+    }
+    return {
+      width: permDrawerWidth.value,
+      overflow: 'hidden',
+      minWidth: 0
+    };
+  }, [drawerType]);
+  const permInnerSlideStyle = useAnimatedStyle(() => {
+    if (drawerType !== 'permanent') {
+      return {};
+    }
+    return {
+      transform: [{
+        translateX: permDrawerInnerX.value
+      }]
+    };
+  }, [drawerType]);
   const contentAnimatedStyle = useAnimatedStyle(() => {
     return {
       // FIXME: Force Reanimated to commit to the shadow tree
@@ -269,7 +316,7 @@ export function Drawer({
             children: [/*#__PURE__*/_jsxs(Animated.View, {
               style: [styles.content, contentAnimatedStyle],
               children: [/*#__PURE__*/_jsx(View, {
-                "aria-hidden": isOpen && drawerType !== 'permanent',
+                "aria-hidden": false,
                 style: styles.content,
                 children: children
               }), drawerType !== 'permanent' ? /*#__PURE__*/_jsx(Overlay, {
@@ -279,11 +326,24 @@ export function Drawer({
                 style: overlayStyle,
                 accessibilityLabel: overlayAccessibilityLabel
               }) : null]
-            }), /*#__PURE__*/_jsx(Animated.View, {
+            }), drawerType === 'permanent' ? /*#__PURE__*/_jsx(Animated.View, {
+              removeClippedSubviews: Platform.OS !== 'ios',
+              style: [styles.drawer, {
+                position: 'relative',
+                zIndex: 1
+              }, drawerStyle, permClipStyle],
+              children: /*#__PURE__*/_jsx(Animated.View, {
+                style: [{
+                  width: drawerWidth,
+                  flexShrink: 0
+                }, permInnerSlideStyle],
+                children: renderDrawerContent()
+              })
+            }) : /*#__PURE__*/_jsx(Animated.View, {
               removeClippedSubviews: Platform.OS !== 'ios',
               style: [styles.drawer, {
                 width: drawerWidth,
-                position: drawerType === 'permanent' ? 'relative' : 'absolute'
+                position: 'absolute'
               }, drawerAnimatedStyle, drawerStyle],
               children: renderDrawerContent()
             })]
@@ -301,10 +361,11 @@ const styles = StyleSheet.create({
     top: 0,
     bottom: 0,
     maxWidth: '100%',
-    backgroundColor: 'white'
+    backgroundColor: 'transparent'
   },
   content: {
-    flex: 1
+    flex: 1,
+    minWidth: 0
   },
   main: {
     flex: 1,
diff --git a/node_modules/react-native-drawer-layout/lib/module/views/Overlay.js b/node_modules/react-native-drawer-layout/lib/module/views/Overlay.js
index abe6647..eab47b1 100644
--- a/node_modules/react-native-drawer-layout/lib/module/views/Overlay.js
+++ b/node_modules/react-native-drawer-layout/lib/module/views/Overlay.js
@@ -1,42 +1,35 @@
 "use strict";
 
-import { Pressable, StyleSheet, View } from 'react-native';
+import { StyleSheet, View } from 'react-native';
 import { jsx as _jsx } from "react/jsx-runtime";
+/**
+ * Pass-through overlay: no scrim and no touch interception so the main screen
+ * stays fully usable while the drawer is open (close via menu / swipe only).
+ */
 export function Overlay({
-  open,
-  onPress,
+  open: _open,
+  onPress: _onPress,
   style,
-  accessibilityLabel = 'Close drawer',
+  accessibilityLabel: _accessibilityLabel = 'Close drawer',
   ...rest
 }) {
   return /*#__PURE__*/_jsx(View, {
     ...rest,
     style: [StyleSheet.absoluteFill, styles.overlay, {
-      opacity: open ? 1 : 0,
-      pointerEvents: open ? 'auto' : 'none'
+      opacity: 0,
+      pointerEvents: 'none'
     }, style],
-    "aria-hidden": !open,
-    children: /*#__PURE__*/_jsx(Pressable, {
-      onPress: onPress,
-      style: [styles.pressable, {
-        pointerEvents: open ? 'auto' : 'none'
-      }],
-      role: "button",
-      "aria-label": accessibilityLabel
-    })
+    "aria-hidden": true
   });
 }
 const styles = StyleSheet.create({
   overlay: {
-    backgroundColor: 'rgba(0, 0, 0, 0.5)',
+    backgroundColor: 'transparent',
     // Disable touch highlight on mobile Safari.
     // WebkitTapHighlightColor must be used outside of StyleSheet.create because react-native-web will omit the property.
     // @ts-expect-error: WebkitTapHighlightColor is web only
     WebkitTapHighlightColor: 'transparent',
     transition: 'opacity 0.3s'
-  },
-  pressable: {
-    flex: 1
   }
 });
 //# sourceMappingURL=Overlay.js.map
diff --git a/node_modules/react-native-drawer-layout/lib/module/views/Overlay.native.js b/node_modules/react-native-drawer-layout/lib/module/views/Overlay.native.js
index 347d806..1c4560b 100644
--- a/node_modules/react-native-drawer-layout/lib/module/views/Overlay.native.js
+++ b/node_modules/react-native-drawer-layout/lib/module/views/Overlay.native.js
@@ -1,48 +1,41 @@
 "use strict";
 
-import { Pressable, StyleSheet } from 'react-native';
+import { StyleSheet } from 'react-native';
 import Animated, { useAnimatedProps, useAnimatedStyle } from 'react-native-reanimated';
 import { jsx as _jsx } from "react/jsx-runtime";
 const PROGRESS_EPSILON = 0.05;
+
+/**
+ * Pass-through overlay: no scrim and no touch interception so the main screen
+ * stays fully usable while the drawer is open (close via menu / swipe only).
+ */
 export function Overlay({
   progress,
-  onPress,
+  onPress: _onPress,
   style,
-  accessibilityLabel = 'Close drawer',
+  accessibilityLabel: _accessibilityLabel = 'Close drawer',
   ...rest
 }) {
   const animatedStyle = useAnimatedStyle(() => {
     return {
-      opacity: progress.value
+      opacity: 0,
     };
-  }, [progress]);
+  }, []);
   const animatedProps = useAnimatedProps(() => {
-    const active = progress.value > PROGRESS_EPSILON;
     return {
-      'pointerEvents': active ? 'auto' : 'none',
-      'aria-hidden': !active
+      'pointerEvents': 'none',
+      'aria-hidden': progress.value <= PROGRESS_EPSILON,
     };
   }, [progress]);
   return /*#__PURE__*/_jsx(Animated.View, {
     ...rest,
     style: [StyleSheet.absoluteFill, styles.overlay, animatedStyle, style],
     animatedProps: animatedProps,
-    children: /*#__PURE__*/_jsx(Pressable, {
-      onPress: onPress,
-      style: styles.pressable,
-      role: "button",
-      "aria-label": accessibilityLabel,
-      accessible: true
-    })
   });
 }
 const styles = StyleSheet.create({
   overlay: {
-    backgroundColor: 'rgba(0, 0, 0, 0.5)'
+    backgroundColor: 'transparent',
   },
-  pressable: {
-    flex: 1,
-    pointerEvents: 'auto'
-  }
 });
 //# sourceMappingURL=Overlay.native.js.map
