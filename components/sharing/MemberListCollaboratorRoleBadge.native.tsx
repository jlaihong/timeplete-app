import type { ReactNode } from "react";
import type { CollaboratorRoleBadgeProps } from "./MemberListCollaboratorRoleBadge.types";

/**
 * Stub for native bundles; `MemberList` only renders this on web.
 */
export function CollaboratorRoleBadge(
  _props: CollaboratorRoleBadgeProps,
): ReactNode {
  return null;
}
